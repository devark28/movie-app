import React from "react";
const Footer = () => (
  <div className="w-full flex justify-between items-center p-4 pt-10 ml-auto">
    <div className="flex text-sm text-gray-400 tracking-wide">
      This site does not store any files on the server, we only linked to the
      media which is hosted on 3rd party services.
    </div>
  </div>
);

export default Footer;
